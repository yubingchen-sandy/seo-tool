pytrends>=4.9.2
pandas>=2.0,<3.0
PyYAML>=6.0
# pytrends 4.9.2 still calls urllib3's deprecated `method_whitelist` kwarg,
# which urllib3 v2 removed. Pin to the v1 line until pytrends ships a fix.
urllib3<2
